query ProductsGetList {
    products {
        id
        name
        price
        description
        categories(first: 1) {
            name
        }
        images(first: 1) {
            url
        }
    }
}