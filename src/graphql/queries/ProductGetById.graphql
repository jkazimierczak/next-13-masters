query ProductGetById($id: ID!) {
    product(where: {id: $id}) {
        id
        name
        price
        description
        categories(first: 1) {
            name
        }
        images(first: 1) {
            url
        }
    }
}